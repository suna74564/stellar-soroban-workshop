[package]
name = "checkin"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["lib", "cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
badge = { path = "../badge" }
soroban-sdk = { workspace = true, features = ["testutils"] }
